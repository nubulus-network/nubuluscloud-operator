# This rule is not used by the project nubuluscloud-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the tunnel.nubulusnetwork.es.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: nubuluscloud-operator
    app.kubernetes.io/managed-by: kustomize
  name: tunnel-editor-role
rules:
- apiGroups:
  - tunnel.nubulusnetwork.es
  resources:
  - tunnels
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - tunnel.nubulusnetwork.es
  resources:
  - tunnels/status
  verbs:
  - get
